# Clinical Master - Python Dependencies
# Separate from root requirements.txt (which has Azure Functions deps)

# Web framework
fastapi
uvicorn[standard]
wsproto  # uvicorn WS backend (websockets 15.x is incompatible — use --ws wsproto)
pydantic>=2.10
pydantic-settings

# AI (realtime patient agent via ADK Gemini Live + feedback via google-genai)
google-adk>=1.5.0
google-genai>=1.0.0

# Utils
python-dotenv

# Database
supabase>=2.0.0
