# Clinical Master - Python Dependencies
# Separate from root requirements.txt (which has Azure Functions deps)

# Web framework
fastapi
uvicorn[standard]
pydantic>=2.10
pydantic-settings

# AI (realtime patient agent + feedback generation via Azure OpenAI)
openai-agents>=0.1.0

# WebSocket client (used by agents.realtime for Azure OpenAI connection)
websockets
wsproto

# Utils
python-dotenv

# Database
supabase>=2.0.0
